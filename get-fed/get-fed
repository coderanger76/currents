#!/bin/bash
# get-fed launcher — activates venv and runs the FRED economic data viewer

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

source "$SCRIPT_DIR/venv/bin/activate"

python "$SCRIPT_DIR/main.py" "$@"
